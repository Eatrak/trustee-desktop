import "./style.css";

const LoadingBar = () => {
    return (
        <div className="loading-bar">
            <div className="loading-bar__bar"></div>
        </div>
    );
};

export default LoadingBar;
